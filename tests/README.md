# Tests

Run all tests:

```bash
pytest
```

Run with coverage:

```bash
pytest --cov=dbs_annotator --cov-report=html
```

## Dependencies

- pytest
- pytest-qt (Qt widget testing)
- pytest-cov (coverage reporting)
