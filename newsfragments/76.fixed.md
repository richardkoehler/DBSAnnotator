Fix `install.ps1` when run with `iex`/`irm`: move install into a nested function so `$PSCmdlet` binds; document `irm … | iex` one-liner and script parameters; drop optional no–Start-Menu shortcut switch.
